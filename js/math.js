function init() {
    console.log("2的5次幂：", Math.pow(2, 5));
    console.log("0.7四舍五入：", Math.round(.7));
    console.log("0.3向上求整：", Math.ceil(.3));
    console.log("0.7向下求整：", Math.floor(.7));
    console.log("-5求绝对值：", Math.abs(-5));
}

init();